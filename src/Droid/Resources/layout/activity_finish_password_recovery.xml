<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	android:orientation="vertical"
	android:layout_width="match_parent"
	android:layout_height="match_parent"
    android:background="@drawable/gradient_background">
    <include
        android:id="@+id/action_bar"
        layout="@layout/action_bar_back_and_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_gravity="center_horizontal"
        android:textColor="@color/applicationWhite"
        android:textSize="14dp"
        android:text="@string/Attention"/>
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="62dp"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp"
        android:layout_gravity="center_horizontal"
        android:gravity="center"
        android:textColor="@color/applicationWhite"
        android:textSize="14dp"
        android:text="@string/PasswordRecoveryOnMail"/>
    <com.google.android.material.button.MaterialButton
        android:id="@+id/finish_password_recovery_button"
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="60dp"
        android:text="@string/LoginToAccount"
        style="@style/Theme.PrankChat.Button.Borderless"/>
    <TextView
        android:id="@+id/show_publication_text_view"
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="30dp"
        android:textColor="@color/applicationWhite"
        android:textSize="16dp"
        android:gravity="center"
        android:autoLink="all"
        android:text="@string/GoToFeed"/>
</LinearLayout>
